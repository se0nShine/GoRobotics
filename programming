#include <stdio.h>
void move(int *xpos, int *ypos);
int main(void)
{
int a = 10;
int b = 20;
printf("현재의 위치 (%d, %d) \n", a, b);
move(&x, &y);
printf("현재의 위치 (%d, %d) \n", a, b);
printf("프로그램을 종료합니다.");
return 0;
}
void move(int *xpos, int *ypos)
{
*xpos = *xpos + 1;
*ypos = *ypos + 1;
}
